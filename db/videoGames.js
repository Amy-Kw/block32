const data = require('./seedData');

// GET - /api/video-games - get all videoGames
async function getAllVideoGames(/* what params do I need? */) {
    try{
        // code this!
    } catch (error) {
        throw error;
    }
}

// GET - /api/video-games/:videoGameId - get videoGame by id
async function getVideoGameById(/* what params do I need? */) {
    try{
        // code this!
    } catch (error) {
        throw error;
    }
}

// POST - /api/video-games - create a new videoGame
async function createVideoGame(/* what params do I need? */) {
    try{
        // code this!
    } catch (error) {
        throw error;
    }
}

// PUT - /api/video-games/:videoGameId - update a videoGame
async function updateVideoGame(/* what params do I need? */) {
    try {
        // code this!
    } catch (error) {
        throw error;
    }
}

// DELETE - /api/video-games/:videoGameId - delete a videoGame
async function deleteVideoGame(/* what params do I need? */) {
    try{
        // code this!
    } catch (error) {
        throw error;
    }
}

module.exports = {
    getAllVideoGames,
    getVideoGameById,
    createVideoGame,
    updateVideoGame,
    deleteVideoGame
}
